algoritmo "procedmento_soma"


var
x , y , R : inteiro

procedimento soma( A, B )
inicio
	escreval("o valor de x, é :" , A)
	escreval("o valor de y, é :" , B)
   R <- A + B
fimprocedimento

inicio
	X <- 5
	y <- 3
	soma(x, y)
	escreval("e a soma deles é:" ,R)
fimalgoritmo